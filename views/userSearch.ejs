<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= locals.title %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #f4f4f4;
      }
      h1 {
        color: #333;
      }
      ul {
        list-style-type: none;
        padding: 0;
      }
      li {
        background: #fff;
        margin: 10px 0;
        padding: 15px;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      .user-info {
        margin-bottom: 5px;
      }
      .user-info span {
        display: block;
        color: #555;
      }
      a {
        display: inline-block;
        margin-top: 20px;
        padding: 10px 15px;
        background: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 5px;
      }
      a:hover {
        background: #0056b3;
      }
    </style>
  </head>
  <body>
    <h1><%= locals.title %></h1>
    <ul>
      <% locals.users.forEach(user => { %>
      <li>
        <div class="user-info">
          <strong>Username:</strong> <%= user.username %>
        </div>
        <div class="user-info"><strong>Email:</strong> <%= user.email %></div>
        <% if (user.age) { %>
        <div class="user-info"><strong>Age:</strong> <%= user.age %></div>
        <% } %> <% if (user.bio) { %>
        <div class="user-info"><strong>Bio:</strong> <%= user.bio %></div>
        <% } %>
      </li>
      <% }) %>
    </ul>
    <a href="/">Go to home</a>
  </body>
</html>
